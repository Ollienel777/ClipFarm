# YOLOv8 pose estimation
ultralytics==8.3.55
torch>=2.1.0
torchvision>=0.16.0

# Vision / video
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Roboflow ball tracking (inference runs models locally via get_model)
# Shared deps are pinned here to prevent inference from upgrading them past
# what ultralytics and inference-sdk require.
aiohttp>=3.9.0,<=3.10.11
pillow>=9.0.0,<11.0
inference>=0.9.0,<0.30.0
python-dotenv>=1.0.0

# Install order matters: pin numpy/pillow/aiohttp before inference, then torch/ultralytics.
# See ml/dead_time/README-venv.md for setup instructions.
